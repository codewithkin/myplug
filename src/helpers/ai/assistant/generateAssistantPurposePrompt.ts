export default async function generatePurposePromptAssistant ({website, purpose, name}: {
    website: string,
    name?: string,
    purpose: string
}) {
    try {
        const instruction = ``;

    } catch (e) {
        console.log("An error occured while generating assistant prompt: ", e);
    }
}